# Core framework
Django>=5.1,<6.0

# REST API
djangorestframework>=3.15,<4.0

# CORS for local dev and embedding widget
django-cors-headers>=4.3,<5.0

# Optional: WSGI server for deployments (not required for dev)
# gunicorn>=22.0,<23.0
